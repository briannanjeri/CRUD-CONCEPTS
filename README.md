# CRUD-CONCEPTS
This project entails CRUD operation, one can create lists with its items, read from the database, update and delete the list.
It is developed using Javascript, Html and css in the frontend while backend it's developed with python and flask microframe work as a tool to develop the servers. used postgresql as DBMS
